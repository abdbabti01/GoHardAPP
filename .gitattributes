# Git hook scripts must keep LF line endings so Git Bash's shell can
# execute them correctly regardless of core.autocrlf on Windows checkouts.
.githooks/* text eol=lf

# Shell scripts run by Xcode and CI must keep LF line endings.
ios/scripts/*.sh text eol=lf
